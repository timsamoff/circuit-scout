# Project Profile — Circuit Scout

Generated by Sentinel 00-project-discovery. Feeds the `[PROJECT-SPECIFIC]` brackets in the Design & Code Quality Audit and Integration Audit prompts.

## Agent identification

- **AI coding agent in use**: Claude Code
- **Project-context file name**: `CLAUDE.md` (Claude Code's native name — none currently exists at the project root; later prompts should write to `CLAUDE.md`, not `AGENTS.md`)
- **Permission/configuration mechanism**: `.claude/settings.json` (not currently present in this project)

## Project name

**Circuit Scout** — confirmed with user. Declared in `package.json` (`"name": "circuit-scout"`) and as the README title.

## Tech stack / primary language(s)

- **Language**: JavaScript (Node.js, ESM — `"type": "module"` in package.json), plain HTML/CSS/vanilla JS on the frontend (no framework, no bundler).
- **Package manager**: npm (`package.json` + `package-lock.json` present).
- **Backend**: Express 4, `sqlite3` for storage (`circuits.db`, gitignored), `axios` + `cheerio` + `xml2js` for scraping RSS/Atom/sitemaps/HTML, `cors`, `dotenv`.
- **Dev tooling**: `nodemon` (dev-only dependency).
- **No test framework detected** (no test script, no `test/` or `__tests__/` directory, no Jest/Mocha/Vitest dependency).

## Application surfaces

Two HTML entry points sharing one backend and one data layer:

1. **Public site** (`index.html`, `js/main.js`, `css/style.css`) — search, filter, and favorite circuits; dark/light mode. Title: "Circuit Scout | DIY Effects Pedal Database".
2. **Admin dashboard** (`admin.html`, `js/admin.js`, `css/admin.css`) — manage feeds, edit circuits, control scraping, ignore-list management. Title: "Circuit Scout Admin | Content Management".

Both surfaces are served by the same `server.js` (Express) against the same SQLite database (`circuits.db`), with `data/circuits.json` exported as a static snapshot for GitHub Pages hosting. These are two views into one shared subsystem, not independent apps — not ambiguous enough to need user confirmation.

Supporting scripts (not separate surfaces, but part of the same data pipeline): `scraper.js`, `force-export.js`, `fix-descriptions.js`, `fix-duplicates.js`, `fix-titles.js`, `compare-db-json.js`.

## Design/token system file

Found — **not ambiguous**: both stylesheets define CSS custom-property blocks at `:root`:
- [css/style.css:31](css/style.css#L31) — `:root,` (public site; ~119 custom-property declarations in the file)
- [css/admin.css:18](css/admin.css#L18) — `:root {` (admin dashboard)

These are the design/token system for the Design & Code Quality Audit's Part 0/Part 2 brackets. No separate `design-tokens.json`, Tailwind config, or platform color-asset catalog exists — the two `:root` blocks are the source of truth. Worth noting for that audit: there are two separate `:root` blocks rather than one shared token file, so token consistency between the public site and admin dashboard is itself worth checking.

## Recurring unit of work

**A circuit entry** (a single DIY effects-pedal circuit record: title, source, effect type, difficulty, components, etc., stored in `circuits.db` and mirrored into `data/circuits.json`). Secondary recurring unit: **a scraper source** (an RSS/Atom feed, sitemap, or static HTML listing registered for scraping). Not ambiguous — the domain model is a straightforward single-entity database-backed catalog with one clear primary record type.

## VCS status

- Git repository: yes.
- Commit count: 45.
- Distinct commit authors: 2 — `Tim Samoff` (human) and `GitHub Action` (the now-removed auto-scrape workflow's commit identity).
- Commit-message convention: **no formal convention in use** — no Conventional Commits prefixes. Subjects are short, plain-English, imperative/past-tense descriptions ("Added clear button to search boxes", "Fixed a favicon typo", "Update icon path to..."). Mixed tense (both "Added X" and "Add X" / "Update X" appear). No commit bodies used — single-line summaries throughout. This matches the user's stated global preference for single-line commit summaries, so the Integration Audit's Part 4 should propose keeping the existing plain-English single-line style rather than introducing a prefix convention.

## Version/release status

- `package.json` version: `1.0.0`.
- No git tags, no GitHub releases detected.
- Reads as an established personal/small-scope project rather than pre-release experimental code — it has a stable "1.0.0" version, a real README with feature list and license, and 45 commits of iterative feature work, but no formal release process (no tags/changelog).

## Existing automation

- **CI**: none currently active. `.github/workflows/scrape.yml.bak` exists (disabled — renamed with `.bak` extension) — a GitHub Actions workflow that ran the scraper on a 12-hour cron and auto-committed/pushed `data/circuits.json` and `circuits.db`. Most recent commit ("Removing scrape.yml from process") indicates this was deliberately taken out of the active pipeline.
- **Git hooks**: none detected (no `.husky/`, no custom `.git/hooks` beyond samples).
- **npm scripts**: `start` (run server), `dev` (nodemon), `scrape` (run scraper manually), `export` (force-export to JSON). No `test`, `lint`, `build`, or `precommit` scripts.
- **No pre-commit framework** (no `.pre-commit-config.yaml`, no `lint-staged`/`husky` in package.json).

## Existing docs and scaffolding

- `AGENTS.md`: not found.
- `README.md`: **exists** (project root) — feature overview, license badge/link, icon.
- `LICENSE`: **exists** (root) — full GPL-3.0 text. Note: this contradicts `package.json`'s `"license": "MIT"` field — flagging as a discrepancy for the user's awareness, not resolving it here since it's outside this discovery prompt's scope.
- `.gitignore`: **exists** — covers `node_modules/`, `package-lock.json` (ignored despite being present and tracked — worth flagging as a possible inconsistency), `circuits.db`, `*.db-journal`, `.env*`, logs, OS/editor cruft. Explicit comment notes `data/circuits.json` is deliberately NOT ignored (needed for GitHub Pages).
- `sentinel-notes/`: not found.
- `scratch/`: not found.
- `production_notes/`: not found (user's usual habit per global CLAUDE.md, but not present in this project yet).
- `Sentinel/`: exists — contains this prompt and its siblings (00–03) plus a Sentinel README. This is the audit toolkit itself, not project documentation.

## Existing design documentation

**None found.** No `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/adr/` directory, or design doc linked from the README. Flag as a gap for the Design & Code Quality Audit — there is no documented-intent baseline to check the implementation against; that audit will need to infer intended behavior from the code and README alone.

---

## How to use this

When running the **Design & Code Quality Audit** (`Sentinel/01-design-quality-audit.md`) or **Integration Audit** (`Sentinel/02-integration-audit.md`) prompts, replace their `[PROJECT-SPECIFIC]` brackets using the corresponding values above rather than re-detecting or guessing fresh:

- Design/token system → the two `:root` blocks in `css/style.css` and `css/admin.css`.
- Recurring unit of work → "a circuit entry" (secondary: "a scraper source").
- Application surfaces → public site (`index.html`) and admin dashboard (`admin.html`), sharing one Express/SQLite backend.
- Commit convention → none formal; keep the existing plain-English single-line style.
- Existing automation → none active (disabled scrape workflow, four manual npm scripts, no CI/tests/hooks).

The **Gate-Check Implementation** prompt (`Sentinel/03-gate-check-implementation.md`) has no brackets of its own but can reuse the "Existing automation" finding above instead of re-detecting it.
