# Project Profile — Circuit Scout

Generated by Sentinel 00-project-discovery. Feeds the `[PROJECT-SPECIFIC]` brackets in the Design & Code Quality Audit and Integration Audit prompts.

**Sentinel version**: 1.0. **This is an update run**, not a fresh run — the prior `PROJECT_PROFILE.md` had no version recorded at all, so it predates this versioning system (pre-1.0) and got the full thorough re-check this prompt calls for in that case, not an incremental diff. Prior generation date: 2026-09-13. This update: 2026-09-14.

**Gate-check hook re-validation flag**: a gate-check system (`.githooks/` + `sentinel-gate/check.js`) was built by the Gate-Check Implementation prompt after the prior profile was generated. Per this version's update-handling instructions, that hook needs re-validation against the *current* version of `03-gate-check-implementation.md`'s requirements before being trusted as-is — it was built under whatever version of that prompt existed on 2026-09-13, which may differ from the current one the same way this discovery prompt itself changed. Not re-validated as part of this discovery pass (out of scope for this prompt) — flag this explicitly for whoever runs the Gate-Check Implementation prompt next.

## Agent identification

- **AI coding agent in use**: Claude Code
- **Project-context file name**: `CLAUDE.md` (Claude Code's native name). **Now exists** at the project root (created by the Design & Code Quality Audit's Part 11 on 2026-09-13) — this differs from the prior profile, which recorded none existing yet.
- **Permission/configuration mechanism**: `.claude/settings.json` — still not present in this project.

## Project name

**Circuit Scout** — unchanged, still confirmed with the user. Declared in `package.json` (`"name": "circuit-scout"`) and as the README title. Not re-asked this run per this version's instruction not to re-confirm answers still valid.

## Tech stack / primary language(s)

- **Language**: JavaScript (Node.js, ESM — `"type": "module"` in package.json), plain HTML/CSS/vanilla JS on the frontend (no framework, no bundler).
- **Package manager**: npm (`package.json` + `package-lock.json` present).
- **Backend**: Express 4, `sqlite3` for storage (`circuits.db`, gitignored), `axios` + `cheerio` + `xml2js` for scraping RSS/Atom/sitemaps/HTML, `cors`, `dotenv`.
- **Dev tooling**: `nodemon` (dev-only dependency).
- **No test framework detected** (no test script, no `test/` or `__tests__/` directory, no Jest/Mocha/Vitest dependency). Unchanged from prior profile.

## Application surfaces

Two HTML entry points sharing one backend and one data layer — unchanged from prior profile:

1. **Public site** (`index.html`, `js/main.js`, `css/style.css`) — search, filter, and favorite circuits; dark/light mode. Title: "Circuit Scout | DIY Effects Pedal Database".
2. **Admin dashboard** (`admin.html`, `js/admin.js`, `css/admin.css`) — manage feeds, edit circuits, control scraping, ignore-list management. Title: "Circuit Scout Admin | Content Management".

Both surfaces are served by the same `server.js` (Express) against the same SQLite database (`circuits.db`), with `data/circuits.json` exported as a static snapshot for GitHub Pages hosting. These are two views into one shared subsystem, not independent apps — not ambiguous enough to need user confirmation.

Supporting scripts (not separate surfaces, but part of the same data pipeline): `scraper.js`, `force-export.js`, `fix-descriptions.js`, `fix-duplicates.js`, `fix-titles.js`, `compare-db-json.js`.

**New since prior profile**: a gate-check subsystem now exists — `sentinel-gate/check.js` plus `.githooks/` — enforcing conventions on commit/push. This isn't an application surface (it doesn't serve the product), but it is new tooling infrastructure worth noting under Existing Automation below.

## Design/token system file

Found — **not ambiguous**, unchanged from prior profile: both stylesheets define CSS custom-property blocks at `:root`:
- [css/style.css:31](css/style.css#L31) — `:root,` (public site)
- [css/admin.css:18](css/admin.css#L18) — `:root {` (admin dashboard)

Still two separate `:root` blocks rather than one shared token file — this gap was found and tracked (`sentinel-notes/TODO.md`'s `tokens` item, with a linked design brief) by the Design & Code Quality Audit, and remains unresolved as of this update.

## Recurring unit of work

**A circuit entry** (unchanged). Secondary: **a scraper source**. Not ambiguous, not re-asked.

## VCS status

- Git repository: yes.
- **Commit count: 55** (was 45 at prior profile generation — 10 new commits since 2026-09-13, covering the design-quality audit's fixes, the integration audit, the gate-check implementation, and the DESIGN.md relocation/rewrite).
- Distinct commit authors: 2 — `Tim Samoff` (human) and `GitHub Action` (the now-removed auto-scrape workflow's commit identity). Unchanged.
- Commit-message convention: **now formally established**, where the prior profile found none. The Integration Audit (2026-09-13) confirmed with the user: terse, plain-English, single-line summaries, no Conventional Commits prefixes, one commit per session/logical change as the default. This is now also **mechanically enforced** by a `commit-msg` git hook (see Existing Automation) — a materially different state than the prior profile's "no formal convention" finding.

## Version/release status

- `package.json` version: `1.0.0`. Unchanged.
- No git tags, no GitHub releases detected. Unchanged.
- Still reads as an established personal/small-scope project with no formal release process — unchanged assessment.

## Existing automation

Materially different from the prior profile — **a real gate-check system now exists**, not just generic npm scripts:

- **Git hooks**: `core.hooksPath` is set to `.githooks` (confirmed via `git config --get core.hooksPath`). Three hooks are wired: `pre-commit`, `commit-msg`, `pre-push`, all sourced from the tracked `.githooks/` directory (not the untracked `.git/hooks/`, which is the correct pattern for hooks meant to survive a clone).
- **This is a genuine, purpose-built gate-check/quality-enforcement system**, not incidental tooling — it was built specifically by Sentinel's own Gate-Check Implementation prompt on 2026-09-13. Its logic lives in `sentinel-gate/check.js` and covers: commit-message format enforcement (subject length, bullet-only body structure, attribution-trailer stripping), several advisory (non-blocking) touchpoint-parity checks (export-field parity across data-export functions, render-function parity between the two surfaces, CLAUDE.md/README-touched reminders, duplicated-detection-logic drift warnings), one blocking check (`sentinel-notes/TODO.md` sync against design-brief files, via working-tree content hashing since `sentinel-notes/` is gitignored and thus invisible to git diffs), and a hook-infrastructure self-check (full-scan mode only) verifying the hooks themselves haven't gone missing or lost their executable bit.
- An exceptions mechanism exists (`sentinel-exceptions.json`, currently an empty array — no active exceptions) plus a `Sentinel-Override:` commit-trailer escape hatch for the commit-message bullet-count cap specifically.
- A baseline file (`sentinel-gate/baseline.json`, tracked in git) records zero pre-existing blocking violations as of the gate's creation.
- **Per this version's update-run instructions, this hook has not been re-validated against the current version of the Gate-Check Implementation prompt** — see the flag at the top of this document.
- **npm scripts**: unchanged — `start`, `dev`, `scrape`, `export`. No `test`, `lint`, `build`, or `precommit` scripts.
- **No separate pre-commit framework** (no husky, no `.pre-commit-config.yaml`) — the hooks are plain Node.js scripts invoked via native git hook mechanics, a deliberate choice confirmed with the user during the Gate-Check Implementation pass (to avoid adding a dependency for a solo, no-build-step project).

## Existing docs and scaffolding

Several fields changed from the prior profile:

- `AGENTS.md`: not found (this project uses Claude Code's native `CLAUDE.md` name instead, per Agent identification above).
- `CLAUDE.md`: **now exists** at the project root, gitignored/local-only per confirmed project preference. This differs from the prior profile, which found none.
- `README.md`: exists (project root) — feature overview, license badge/link, icon. License section confirmed still correctly pointing to MIT.
- `LICENSE`: exists (root) — MIT, consistent with `package.json` and every file header's `@license` tag. The GPL-3.0/MIT conflict noted in the prior profile was resolved during the Design & Code Quality Audit and remains resolved.
- `.gitignore`: exists — same core exclusions as before (`node_modules/`, `circuits.db`, `.env*`, OS/editor cruft), plus new entries added since the prior profile: `CLAUDE.md`, `sentinel-notes/`, `Sentinel/`, `scratch/`, and `sentinel-gate/notes-snapshot.json`. The prior profile's flagged inconsistency — `package-lock.json` gitignored despite being present and tracked in git — is **still present, unchanged**.
- `sentinel-notes/`: **now exists**, gitignored per project preference. Contains the Design & Code Quality Audit's full report (`01-design-quality-audit-report.md`), four linked design briefs, and `TODO.md` (currently tracking action items from that audit).
- `scratch/`: **now exists**, gitignored per project preference, for throwaway verification scripts. **Flagged for follow-up** below — currently contains a leftover test file that should have been cleaned up.
- `Sentinel/`: exists — the audit-prompt toolkit itself (00-03 plus a README), gitignored per project preference (not project documentation, and not re-described in detail here per this prompt's own scope).

## Existing design documentation

**Changed from the prior profile**, which found none. A design document **now exists**: [docs/DESIGN.md](docs/DESIGN.md), created during the Design & Code Quality Audit (2026-09-13) and subsequently moved from the repo root into `docs/` and rewritten (removing blanket reverse-engineering disclaimer language) at the user's request. This is real grounding material the Design & Code Quality Audit should use as an intent baseline on any future re-run, per this prompt's Part 1 guidance — a different question from the audit's own internal-consistency checks.

## Flagged for follow-up

Incidental findings noticed during this discovery pass that are out of this prompt's own scope, recorded here per this version's instruction so they aren't lost before being triaged into `sentinel-notes/TODO.md`:

- **`scratch/hook-test.txt`** — a leftover throwaway file from testing the gate-check hooks during the 2026-09-13 session, never cleaned up. Harmless (gitignored, so it was never committed) but should be deleted as routine scratch-directory hygiene.
- **`package-lock.json` is gitignored despite being present and tracked in git** — the same inconsistency the prior profile flagged on 2026-09-13, still unresolved. Worth a real decision (either stop tracking it, since it's gitignored, or remove it from `.gitignore` since it's clearly meant to be tracked) rather than leaving the contradiction in place indefinitely.

---

## How to use this

When running the **Design & Code Quality Audit** (`Sentinel/01-design-quality-audit.md`) or **Integration Audit** (`Sentinel/02-integration-audit.md`) prompts, replace their `[PROJECT-SPECIFIC]` brackets using the corresponding values above rather than re-detecting or guessing fresh:

- Design/token system → the two `:root` blocks in `css/style.css` and `css/admin.css` (still unconsolidated — tracked in `sentinel-notes/TODO.md`).
- Recurring unit of work → "a circuit entry" (secondary: "a scraper source").
- Application surfaces → public site (`index.html`) and admin dashboard (`admin.html`), sharing one Express/SQLite backend.
- Commit convention → now formally established AND mechanically enforced (terse single-line, no prefixes) — a materially different starting point than the prior profile's "no formal convention."
- Existing automation → a real gate-check system now exists (`.githooks/` + `sentinel-gate/check.js`); this is not a blank slate for the Gate-Check Implementation prompt.
- Existing design documentation → `docs/DESIGN.md` exists and should ground any future audit's intent-vs-implementation check.

The **Gate-Check Implementation** prompt (`Sentinel/03-gate-check-implementation.md`), if run again, should treat this as an **update to an existing gate-check system**, not a fresh build — and should specifically re-validate the existing hooks against whatever the current version of that prompt requires, per the flag at the top of this document.

Next step in the sequence, per this prompt's own closing instruction: run the Design & Code Quality Audit next if a fresh audit pass is wanted — though given how recently the prior full audit ran (2026-09-13) and how much of its output is still active/unresolved (`sentinel-notes/TODO.md` has 22 open items as of that audit), a full re-audit may be premature until more of that backlog is addressed. That's a judgment call for the user, not a decision this discovery pass makes on its own.
